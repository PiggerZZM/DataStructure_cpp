#include "SeqQueue.h"
#include <iostream>
#include "LinkQueue.h"
#include "PQueue.h"
using namespace std;

int main()
{
    /*
    // 顺序循环队列测试
    int x,y;
    SeqQueue<int> sq;
    sq.EnQueue(10);
    sq.EnQueue(20);
    sq.DeQueue(x);
    cout << x << endl;
    y = sq.IsEmpty();
    cout << y << endl;
    

   // 链式队列测试
    int x,y;
    LinkQueue<int> lq;
    lq.EnQueue(10);
    lq.EnQueue(20);
    lq.DeQueue(x);
    cout << x << endl;
    y = lq.IsEmpty();
    cout << y << endl;
    

   // 测试优先队列
   PQueue<int> pq;
   pq.Insert(10);
   pq.Insert(30);
   pq.Insert(20);
   int x;
   pq.RemoveMin(x);
   cout << x << endl;
    */

    return 0;
}